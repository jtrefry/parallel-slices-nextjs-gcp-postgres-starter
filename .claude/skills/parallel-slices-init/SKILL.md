---
name: parallel-slices-init
description: Initialize a human-directed, AI-built Next.js GCP application with Claude Code in a universal Parallel Slices repository.
disable-model-invocation: true
---

# Initialize a Next.js GCP project

Run `node scripts/parallel-slices/agent-profile.mjs require-enabled claude-code`
before taking any other action. Stop if Claude Code support is not enabled.

Read `docs/parallel-slices/initialize-nextjs-gcp-project.md` completely and
follow it as the authoritative initialization workflow.

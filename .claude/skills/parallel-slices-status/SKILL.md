---
name: parallel-slices-status
description: Show read-only total and per-slice progress for an active Parallel Slices run, including recovery guidance from durable and local tracking evidence.
disable-model-invocation: true
---

# Show Parallel Slices status

Run `node scripts/parallel-slices/agent-profile.mjs require-enabled claude-code`
before taking any other action. Read
`docs/parallel-slices/check-run-status.md` completely and follow it. Return the
command's status report without modifying the repository, runtime tracking,
run lease, worktrees, pipelines, or reviews.

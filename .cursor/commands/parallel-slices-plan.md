# Plan a Parallel Slices milestone

Run `node scripts/parallel-slices/agent-profile.mjs require-enabled cursor`, read
`.cursor/skills/parallel-slices-plan/SKILL.md` completely, and follow
it.

Create and present the human-readable Product Plan alone for approval. After
approval, commit it separately, AI-compile its requirements into version 2
manifests and version 4 JSON state, validate the dependency graph, and show the
Ready Slices as informational output. Do not implement application code or
start `/loop`.

# Show Parallel Slices status

Run `node scripts/parallel-slices/agent-profile.mjs require-enabled cursor`
before taking any other action. Read
`docs/parallel-slices/check-run-status.md` completely and follow it. Return the
command's status report without modifying the repository, runtime tracking,
run lease, worktrees, pipelines, or reviews.

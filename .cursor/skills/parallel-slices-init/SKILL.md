---
name: parallel-slices-init
description: Initialize a human-directed, AI-built Next.js GCP application with Cursor in a universal Parallel Slices repository.
---

# Initialize a Next.js GCP project

Run `node scripts/parallel-slices/agent-profile.mjs require-enabled cursor`
before taking any other action. Stop if Cursor support is not enabled.

Read `docs/parallel-slices/initialize-nextjs-gcp-project.md` completely and
follow it as the authoritative initialization workflow.
